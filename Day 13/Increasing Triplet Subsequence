class Solution:
    def increasingTriplet(self, nums):
        a=b=10**18
        for x in nums:
            if x<=a: a=x
            elif x<=b: b=x
            else: return True
        return False
