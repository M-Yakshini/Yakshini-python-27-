class Solution:
    def maxSubArrayLen(self, nums, k):
        d={0:-1}
        s=0; r=0
        for i,x in enumerate(nums):
            s+=x
            if s-k in d:
                r=max(r,i-d[s-k])
            if s not in d:
                d[s]=i
        return r
