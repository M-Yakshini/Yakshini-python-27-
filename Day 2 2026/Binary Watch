class Solution:
    def readBinaryWatch(self, t):
        r=[]
        for h in range(12):
            for m in range(60):
                if bin(h).count('1')+bin(m).count('1')==t:
                    r.append(f"{h}:{m:02d}")
        return r
