class Solution:
    def maxRotateFunction(self, nums):
        s=sum(nums)
        f=sum(i*x for i,x in enumerate(nums))
        r=f
        n=len(nums)
        for i in range(n-1,0,-1):
            f+=s-n*nums[i]
            r=max(r,f)
        return r
