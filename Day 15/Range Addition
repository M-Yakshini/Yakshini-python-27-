class Solution:
    def getModifiedArray(self, n, updates):
        a=[0]*(n+1)
        for l,r,v in updates:
            a[l]+=v
            if r+1<n: a[r+1]-=v
        for i in range(1,n):
            a[i]+=a[i-1]
        return a[:n]
