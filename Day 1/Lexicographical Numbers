Lexicographical Numbersclass Solution:
    def lexicalOrder(self, n):
        r=[]
        def dfs(x):
            if x>n: return
            r.append(x)
            for i in range(10):
                dfs(x*10+i)
        for i in range(1,10):
            dfs(i)
        return r
