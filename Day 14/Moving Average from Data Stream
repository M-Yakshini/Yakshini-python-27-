class MovingAverage:
    def __init__(self, size):
        self.s=size
        self.q=[]
        self.t=0
    def next(self, val):
        self.q.append(val)
        self.t+=val
        if len(self.q)>self.s:
            self.t-=self.q.pop(0)
        return self.t/len(self.q)
