class Solution:
    def __init__(self, nums):
        self.a=nums
    def pick(self, t):
        import random
        r=-1; c=0
        for i,x in enumerate(self.a):
            if x==t:
                c+=1
                if random.randrange(c)==0:
                    r=i
        return r
